SFTP_ROUTER=
SFTP_USERNAME=
SFTP_PASSWORD=
# always (default) = idefix-server is running after every sync, started even
# if it was down | auto = only restart it if it was already running
# never = don't touch it
IDEFIX_RESTART=always
