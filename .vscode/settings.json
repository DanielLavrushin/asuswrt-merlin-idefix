{
  "explorer.fileNesting.enabled": true,
  "explorer.fileNesting.patterns": {
    "*.tsx": "${capture}.*",
    "Dockerfile": "Dockerfile.*",
    "electron.bootstrap.js": "electron${capture}.*"
  }
}
